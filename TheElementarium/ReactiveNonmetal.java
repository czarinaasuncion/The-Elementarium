package TheElementarium;

import TheElementarium.elements.Element;

public class ReactiveNonmetal extends Element {
    public ReactiveNonmetal(String name, String symbol, int atomicNumber, double atomicWeight,
                            String electronConfig, String applications) {
        super(name, symbol, atomicNumber, atomicWeight, "Reactive nonmetals", electronConfig, applications);
    }
}
